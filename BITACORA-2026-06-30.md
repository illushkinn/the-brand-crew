# Bitácora - 30 de Junio 2026

## Resumen del día
Sesión enfocada en code review, eliminación de dead code, refactor DRY/clean, y deploy. También se iteró el diseño de la flecha hand-drawn del preloader.

## Lo hecho

- **Preloader arrow v3**: shaft con S-curve más marcada (4 curvas bezier), head con alas curvas + base recta cerrando triángulo, stroke-dashoffset animation secuencial (shaft 0.55s → head 0.4s)
- **FAQ padding fix**: `.faq-list` margin-top de 3rem → 1.5rem (elimina gap excesivo arriba de "What if I don't like it?")
- **OKLCH compliance**: 4 instancias de `rgba(179,107,72)` reemplazadas por `oklch(0.58 0.18 32 / X)` — corrige violación del CLAUDE.md
- **Dead code removido**:
  - `.text-ochra` + `--ochra` alias (sin uso)
  - `--text` alias reemplazado por `var(--body)` directo
  - `.mobile-link-empecemos` (selector sin elemento HTML)
  - `.resultado-historia blockquote` (sin `<blockquote>` en HTML)
- **A11y fixes**:
  - `scroll-to-top`: `<div role=button tabindex=0>` → `<button>` nativo
  - Inline `onclick`/`onkeydown` reemplazados por `addEventListener`
  - `.progreso-tag` font-size 0.6rem → 0.7rem
  - Removidos `document.activeElement.blur()` redundantes
- **DRY**: `backdrop-filter` extraído a variable `--glass-effect`
- **Clean code**: variable `p` renombrada a `preloaderEl`

## Deploy
- Commit `c84a504` → `vercel deploy --prod`
- Production: https://thebrandcrew.lat (8s deploy, static site)

## Pendiente
- Review visual de la flecha hand-drawn del preloader
- Focus trap en menú mobile (a11y high priority)
- Los otros proyectos: onboardings, Apolonia, rotisería, contenido TBC, VERIFACT, Pragma
