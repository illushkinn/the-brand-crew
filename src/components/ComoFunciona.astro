---
// ComoFunciona.astro - How it works section with 3 step cards
import type { Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/ui';

interface Props { lang?: Lang }
const { lang = 'es' } = Astro.props;
const t = useTranslations(lang);
---

<section class="section como-funciona reveal" id="resultados" aria-label="How it works">
  <span class="section-label mono text-verde">{t('comoFunciona.kicker')}</span>
  <h2 class="section-title">{t('comoFunciona.title1')}<br><span class="text-verde">{t('comoFunciona.title2')}</span>.</h2>
  <p class="section-sub" set:html={t('comoFunciona.sub')}></p>
  <div class="como-funciona-grid">
    <div class="glass-card como-funciona-card">
      <h3>{t('comoFunciona.step1Title')}</h3>
      <p>{t('comoFunciona.step1Text')}</p>
    </div>
    <div class="glass-card como-funciona-card">
      <h3>{t('comoFunciona.step2Title')}</h3>
      <p>{t('comoFunciona.step2Text')}</p>
    </div>
    <div class="glass-card como-funciona-card">
      <h3>{t('comoFunciona.step3Title')}</h3>
      <p>{t('comoFunciona.step3Text')}</p>
    </div>
  </div>
</section>
