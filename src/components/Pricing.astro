---
// Pricing.astro - Pricing section: Kit 1 (Web Design) + link to Growth Partner
import type { Lang } from '../i18n/ui';
import { useTranslations } from '../i18n/ui';
import PricingKit1 from './PricingKit1.astro';
import Icon from './Icon.astro';

interface Props { lang?: Lang }
const { lang = 'es' } = Astro.props;
const t = useTranslations(lang);

const growthLink = lang === 'es' ? '/pricing/growth-partner' : '/en/pricing/growth-partner';
---

<section class="section pricing reveal-stagger" id="precios" aria-label="Pricing">
  <span class="section-label mono text-terracota rv">{t('pricing.kicker')}</span>
  <h2 class="section-title rv">{t('pricing.title1')} <span class="text-verde">{t('pricing.title2')}</span>.</h2>

  <div class="rv"><PricingKit1 lang={lang} /></div>

  <!-- Growth Partner CTA -->
  <div class="pricing-growth-cta rv">
    <p class="pricing-growth-cta-text">{t('pricing.growthCtaText')}</p>
    <a href={growthLink} class="btn btn-outline">
      {t('pricing.growthCtaLink')}
      <Icon name="arrow-right" size={16} strokeWidth={2.5} />
    </a>
  </div>
</section>
